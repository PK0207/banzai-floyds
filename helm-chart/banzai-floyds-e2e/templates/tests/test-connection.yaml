apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "banzai-floyds-e2e.fullname" . }}-test-connection"
  labels:
{{ include "banzai-floyds-e2e.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "banzai-floyds-e2e.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
